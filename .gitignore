**/__pycache__
**/*.egg-info
src/fmfts/datasets
**/runs
**/checkpoints
*.pt
# Local notes (ignored except meeting.md)
# Use a file-level ignore so the directory itself is not excluded,
# allowing the exception to work.
notes/*
!notes/meeting.md
*.out