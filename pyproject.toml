[project]
name = "fmfts"
version = "0.1.0"
# authors = [{ name = "" }]
description = ""
readme = ""
dependencies = []

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"

[tool.setuptools]
package-dir = {"" = "src"}

[tool.setuptools.packages.find]
where = ["src"]